// ignore_for_file: file_names

void main() {
  final age = 12;

  // (condição) ? faça algo : outra coisa
  final isUnderAge = age < 18 ? "Menor de idade" : "Maior de idade";

  print(isUnderAge);
}
